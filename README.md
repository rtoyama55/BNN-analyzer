# BNN-analyzer

## Abstract
This repository contains the simulation code
We analyze the attractor dynamics and fixed points in a binary neural network model.

## Requirements
* Python 3.10.9
* numpy
* matplotlib
* colorama

## Usage
You can configure parameters in the `Config` class within `main.py`.

Run the simulation:
```bash
python main.py
